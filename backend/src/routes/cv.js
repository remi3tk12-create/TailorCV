import { Router } from 'express';
// import { uploadOriginalCv, generateTailoredCv, getCvHistory, getCvDetails } from '../controllers/cvController.js';
// import { authenticateToken } from '../middleware/authMiddleware.js';
// import { upload } from '../middleware/uploadMiddleware.js';

const router = Router();

// router.post('/upload', authenticateToken, upload.single('cv'), uploadOriginalCv);
// router.post('/generate', authenticateToken, generateTailoredCv);
// router.get('/history', authenticateToken, getCvHistory);
// router.get('/:id', authenticateToken, getCvDetails);

export default router;
