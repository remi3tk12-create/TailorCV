import { Router } from 'express';
// import { scanCompanyWebsite } from '../controllers/companyController.js';
// import { authenticateToken } from '../middleware/authMiddleware.js';

const router = Router();

// router.post('/scan', authenticateToken, scanCompanyWebsite);

export default router;
